# URL of the backend API server.
# Default (empty string) means the frontend hits the same host via Vite's proxy (/api → localhost:5001).
# Override only if your backend runs on a different host or port.
VITE_BACKEND_URL=
